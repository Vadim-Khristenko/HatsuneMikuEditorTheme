plugins {
    // Auto-provision JDK toolchains (local machines may not have JDK 21)
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "hatsune-miku-theme"
